namespace Dotlyzer.Libraries
{
   #region Classes

   internal static class Constants // Fully static, stateless, implicitly abstract, implicitly sealed and no [default] constructor.
   {
      #region Fields

      // Information

      //    General
      internal const string NAME = "Dotlyzer";
      internal const string DESCRIPTION = "CLI & interactive based process analyzer and diagnostic tool.";
      internal const string VERSION = "1.0.0.1";

      #endregion Fields
   }

   #endregion Classes
}
